import dotenv from 'dotenv';
// SP4/H4 p.391 TID AW2 "Variables de entorno"  

// Levantamos la configuración de las variables de entorno del archivo .env
// -> .env no se debe subir a un repositorio, ej. github -> configurarlo en .gitignore
dotenv.config();